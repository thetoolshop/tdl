{
  "name": "@repro/tdl",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "inputs": ["{projectRoot}/src/cli/grammar.ohm"],
      "outputs": ["{projectRoot}/src/cli/grammar.ohm-bundle.*"]
    }
  }
}
